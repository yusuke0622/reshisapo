<div class="container">
    <div class="d-flex flex-column align-items-center text-center justify-content-center">
        <h3>本当に退会しますか？</h3>
        <p>※退会によって登録された情報は消去されます</p>
        <div class="row">
        <%= link_to '退会しない', my_page_path(current_user), class: 'btn btn-primary' %>
        <%= link_to '退会する', users_withdraw_path(current_user), method: :PATCH, data: {confirm: "本当に退会してよろしいですか？"}, class: 'btn btn-danger' %>
        </div>
    </div>
</div>
